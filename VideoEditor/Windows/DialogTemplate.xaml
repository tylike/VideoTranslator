<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:VideoEditor.Windows">

    <Style TargetType="{x:Type local:DialogTemplate}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:DialogTemplate}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollViewer Grid.Row="0"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ContentPresenter Content="{TemplateBinding Content}" Margin="20"/>
                        </ScrollViewer>

                        <Border Grid.Row="1"
                                Background="LightGray"
                                BorderBrush="Gray"
                                BorderThickness="0,1,0,0">
                            <ContentPresenter Content="{TemplateBinding Buttons}"
                                              HorizontalAlignment="Right"
                                              Margin="20,10,20,10"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
