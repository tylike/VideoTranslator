<Window x:Class="VideoEditor.Windows.ConsoleMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VideoTranslator Console" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="VideoTranslator Console" FontSize="24" FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <Button x:Name="NewProjectButton" Content="新建项目" Height="40" Margin="0,10" FontSize="16" Click="NewProjectButton_Click"/>
            <Button x:Name="YouTubeImportButton" Content="从YouTube导入" Height="40" Margin="0,10" FontSize="16" Click="YouTubeImportButton_Click"/>
            <Button x:Name="SelectProjectButton" Content="选择项目" Height="40" Margin="0,10" FontSize="16" Click="SelectProjectButton_Click"/>
            <Button x:Name="ExitButton" Content="退出" Height="40" Margin="0,10" FontSize="16" Click="ExitButton_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="请选择操作" HorizontalAlignment="Center" Foreground="Gray" Margin="0,20,0,0"/>
    </Grid>
</Window>
