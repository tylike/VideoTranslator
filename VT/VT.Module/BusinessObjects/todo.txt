1.翻译时，每个批次完成后，没有立即更新到数据？  V
2.tts每一次有时会失败，应重试，最多重试3次。V
每一条完成后，不是立即更新数据的？ V

3.英文音频分段时，是否有可能一条语句，一次分段，返回多个结果？（这样更快）

4.增加一个 步骤10，它是连续执行了8,9，但需要考虑，是否要以将多条命令合并到一起（提速）V
5.调用翻译时的默认提示词设置，如用沙雕风格翻译。V
修改程序前先进行修改文件的备份。